fn main() {
    let ~count : int = 10;

    while count > 0 {
        println(count);
        let ~wait : str = input("Press Enter for next tick...");
        count -= 1;
    }

    println("Liftoff!");


    return;
}
